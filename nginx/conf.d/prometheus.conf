# =============================================================================
# Prometheus Domain Site Configuration: prometheus.internexus.tech
# =============================================================================

upstream prometheus_service {
    server prometheus:9090;
}

server {
    listen 80;
    listen [::]:80;
    server_name prometheus.internexus.tech;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        limit_req zone=general_limit burst=10 nodelay;

        proxy_pass http://prometheus_service;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# HTTPS Server (Port 443) - Prepared SSL Block
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name prometheus.internexus.tech;
#
#     ssl_certificate /etc/letsencrypt/live/prometheus.internexus.tech/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/prometheus.internexus.tech/privkey.pem;
#     ssl_session_timeout 1d;
#     ssl_session_cache shared:SSL:10m;
#     ssl_protocols TLSv1.2 TLSv1.3;
#
#     location / {
#         proxy_pass http://prometheus_service;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#     }
# }
